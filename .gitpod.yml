tasks:
  - name: Install frontend dependencies
    init: cd client; yarn;
    command: yarn dev

ports:
  - port: 3000
    onOpen: open-preview

vscode:
  extensions:
    - ms-azuretools.vscode-docker
    - ms-vscode.makefile-tools
