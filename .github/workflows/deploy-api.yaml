name: api-deploy

on:
  workflow_dispatch:
  
jobs:
  deploy-aPI:
    runs-on: ubuntu-latest
    steps:
      - name: Triggering deploy hook
        run: |
          curl --request GET --url ${{ vars.ENV_DEPLOY_API_URL }}